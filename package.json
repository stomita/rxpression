{
  "name": "rxpression",
  "version": "0.1.0",
  "description": "A simple Reactive Extension combinator using a JavaScript-subset expression language",
  "author": "Shinichi Tomita <shinichi.tomita@gmail.com>",
  "license": "MIT",
  "main": "lib/rxpression.js",
  "scripts": {
    "build": "babel src/ --source-maps inline --out-dir lib/",
    "clean": "rm -rf lib/*",
    "watch": "watch 'npm run build' src/",
    "test": "mocha --compilers js:babel/register --require espower-babel/guess --timeout 20000"
  },
  "files": [
    "README.md",
    "package.json",
    "lib"
  ],
  "devDependencies": {
    "babel": "^5.0.12",
    "espower-babel": "^1.4.1",
    "mocha": "^2.2.4",
    "power-assert": "^0.10.2",
    "promise": "^7.0.0",
    "watch": "^0.16.0"
  },
  "dependencies": {
    "esprima": "^2.1.0",
    "rx": "^2.5.1"
  }
}
